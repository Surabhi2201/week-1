#include <stdio.h>
int main()
{
 float a,b,c;
 int d;
 printf("Enter two floating numbers a and b:");
 scanf("%f%f",&a,&b);
 d=a*b;
 c=a*b;
 printf("The multiplication of two numbers int form: %d",d);
 printf("The multiplication of two numbers floating form:, %f",c);
 return(0);
}
